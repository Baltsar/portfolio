{
  "name": "gustaf-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "fetch-content": "node scripts/fetch-sanity.js",
    "prebuild": "node scripts/fetch-sanity.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@sanity/client": "^7.3.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
